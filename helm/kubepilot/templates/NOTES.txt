KubePilot has been deployed successfully!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Access the UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if .Values.ingress.enabled }}
  URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ (index .Values.ingress.hosts 0).host }}
{{- else }}
  Port-forward to access locally:

    kubectl port-forward -n {{ .Release.Namespace }} \
      svc/{{ include "kubepilot.fullname" . }}-frontend 3000:80

  Then open: http://localhost:3000
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Admin credentials (first run)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Email: {{ .Values.admin.email }}

{{- if .Values.admin.password }}
  Password: (set via admin.password in values.yaml)
{{- else }}
  Password was auto-generated. Retrieve it from pod logs:

    kubectl logs -n {{ .Release.Namespace }} \
      deploy/{{ include "kubepilot.fullname" . }}-backend \
      | grep -A2 "First-Run Setup"
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Integration token (connect remote clusters)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.integrationToken.create }}

  A ServiceAccount token has been created to allow KubePilot to watch
  Kubernetes clusters. Use this token in the Integrations UI.

  Retrieve the token:

    kubectl get secret {{ include "kubepilot.fullname" . }}-integration-token \
      -n {{ .Release.Namespace }} \
      -o jsonpath='{.data.token}' | base64 -d

  To register a remote cluster, create the same ServiceAccount on the
  remote cluster and provide the token + API server URL in the UI.
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Local cluster auto-registration
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.config.inCluster }}

  IN_CLUSTER=true — the cluster "{{ .Values.config.clusterName }}" will be
  auto-registered on first startup using the pod's ServiceAccount token.
  No additional configuration is needed to monitor this cluster.
{{- end }}
